apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-operator
spec:
  template:
    spec:
      containers:
      - name: manager
        image: ghcr.io/containersolutions/redis-operator:<IMAGE>

